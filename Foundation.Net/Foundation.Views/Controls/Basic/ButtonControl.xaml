<Button
    x:Class="Foundation.Views.ButtonControl"
    Name="ThisControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

    mc:Ignorable="d"
    d:DesignHeight="25"
    d:DesignWidth="75"
    HorizontalAlignment="{Binding ElementName=ThisControl, Path=HorizontalAlignment}"
    VerticalAlignment="{Binding ElementName=ThisControl, Path=VerticalAlignment}"
    >

    <Button.Resources>
        <SolidColorBrush x:Key="MouseOverBackgroundKey" Color="Chocolate" />
        <SolidColorBrush x:Key="MouseOverForegroundKey" Color="Yellow" />
        <SolidColorBrush x:Key="DefaultBackgroundKey" Color="{DynamicResource {x:Static SystemColors.ControlColorKey}}" />
        <SolidColorBrush x:Key="DefaultForegroundKey" Color="{DynamicResource {x:Static SystemColors.ControlTextColorKey}}" />
    </Button.Resources>

    <Button.Style>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="Black"
                            BorderThickness="0">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Content="{Binding ElementName=ThisControl, Path=Content}"
                                RecognizesAccessKey="True" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="False" />
                        <Condition Property="IsEnabled" Value="True" />
                    </MultiTrigger.Conditions>
                    <MultiTrigger.Setters>
                        <Setter Property="Background" Value="{DynamicResource DefaultBackgroundKey}" />
                        <Setter Property="Foreground" Value="{DynamicResource DefaultForegroundKey}" />
                    </MultiTrigger.Setters>
                </MultiTrigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsFocused" Value="True" />
                    </MultiTrigger.Conditions>
                    <MultiTrigger.Setters>
                        <Setter Property="Background" Value="{DynamicResource MouseOverBackgroundKey}" />
                        <Setter Property="Foreground" Value="{DynamicResource MouseOverForegroundKey}" />
                    </MultiTrigger.Setters>
                </MultiTrigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="True" />
                        <Condition Property="IsEnabled" Value="True" />
                    </MultiTrigger.Conditions>
                    <MultiTrigger.Setters>
                        <Setter Property="Background" Value="{DynamicResource MouseOverBackgroundKey}" />
                        <Setter Property="Foreground" Value="{DynamicResource MouseOverForegroundKey}" />
                    </MultiTrigger.Setters>
                </MultiTrigger>
            </Style.Triggers>
        </Style>
    </Button.Style>
</Button>
