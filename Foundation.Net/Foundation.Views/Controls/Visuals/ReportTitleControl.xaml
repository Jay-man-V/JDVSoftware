<UserControl
    x:Class="Foundation.Views.ReportTitleControl"
    Name="ThisControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignWidth="250"
    >

    <UserControl.Resources>
        <ResourceDictionary>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <!-- Title and Sub-Title -->
            <RowDefinition Height="Auto" />
            <!-- Dark Separator -->
            <RowDefinition Height="4" />
            <!-- Light Separator -->
            <RowDefinition Height="4" />
            <!-- Data Context -->
            <!--<RowDefinition Height="Auto" />-->
        </Grid.RowDefinitions>

        <!-- Title and Sub-Title -->
        <Grid Row="0">
            <Grid.ColumnDefinitions>
                <!-- Company Icon -->
                <ColumnDefinition Width="26"/>
                <!-- Title and Sub-Title -->
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Company Icon, extracted/used from a Font -->
            <TextBlock 
                Grid.Column="0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Foreground="Blue"
                FontSize="20"
                FontFamily="Times New Roman"
                Text="(o)"
            />

            <!-- Title and Sub-Title grid -->
            <Grid
                Grid.Column="1"
                Margin="8, 0, 0, 4">

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Text="{Binding Title, ElementName=ThisControl}"
                />

                <TextBlock
                    Grid.Row="1"
                    Margin="0, 4, 0, 0"
                    Text="{Binding SubTitle, ElementName=ThisControl}"
                />
            </Grid>
        </Grid>

        <!-- Dark Line -->
        <Rectangle Grid.Row="1">
            <Rectangle.Fill>
                <SolidColorBrush Color="DarkBlue" />
            </Rectangle.Fill>
        </Rectangle>

        <!-- Light Line -->
        <Rectangle Grid.Row="2">
            <Rectangle.Fill>
                <SolidColorBrush Color="LightBlue" />
            </Rectangle.Fill>
        </Rectangle>
    </Grid>
</UserControl>
