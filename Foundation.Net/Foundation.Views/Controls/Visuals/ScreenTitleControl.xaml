<UserControl
    x:Class="Foundation.Views.ScreenTitleControl"
    Name="ThisControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"

    xmlns:fEnums="clr-namespace:Foundation.Interfaces;assembly=Foundation.Interfaces"
    
    Height="40"
    mc:Ignorable="d"
    d:DesignWidth="250"
    >

    <UserControl.Style>
        <Style TargetType="{x:Type UserControl}">
            <Style.Triggers>
                <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.None}">
                    <Setter Property="Background" Value="{StaticResource DefaultWindowTitleBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Asterisk}">
                    <Setter Property="Background" Value="{StaticResource ErrorWindowTitleBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Error}">
                    <Setter Property="Background" Value="{StaticResource ErrorWindowTitleBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Exclamation}">
                    <Setter Property="Background" Value="{StaticResource ErrorWindowTitleBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Hand}">
                    <Setter Property="Background" Value="{StaticResource ErrorWindowTitleBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Information}">
                    <Setter Property="Background" Value="{StaticResource DefaultWindowTitleBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Question}">
                    <Setter Property="Background" Value="{StaticResource QuestionWindowTitleBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Stop}">
                    <Setter Property="Background" Value="{StaticResource ErrorWindowTitleBrush}"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Warning}">
                    <Setter Property="Background" Value="{StaticResource WarningWindowTitleBrush}"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Style>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="GridImageColumn" Width="42" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Image
            Grid.Column="0"
            Margin="5,0,5,0" Width="42" Height="32"
            >
            <Image.Style>
                <Style TargetType="{x:Type Image}">

                <Style.Triggers>
                        <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.None}">
                            <Setter Property="Source" Value="{StaticResource BlankIcon}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Asterisk}">
                            <Setter Property="Source" Value="{StaticResource ErrorIcon}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Error}">
                            <Setter Property="Source" Value="{StaticResource ErrorIcon}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Exclamation}">
                            <Setter Property="Source" Value="{StaticResource ErrorIcon}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Hand}">
                            <Setter Property="Source" Value="{StaticResource ErrorIcon}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Information}">
                            <Setter Property="Source" Value="{StaticResource InformationIcon}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Question}">
                            <Setter Property="Source" Value="{StaticResource AboutIcon}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Stop}">
                            <Setter Property="Source" Value="{StaticResource ErrorIcon}"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding MessageBoxImage}" Value="{x:Static fEnums:MessageBoxImage.Warning}">
                            <Setter Property="Source" Value="{StaticResource WarningIcon}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Image.Style>
        </Image>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <TextBlock
                Margin="2,0,0,0"
                x:Name="ScreenNameTextBox"
                Grid.Row="0"
                VerticalAlignment="Center"
                HorizontalAlignment="Left"
                Text="Screen Name"
                FontSize="{DynamicResource ScreenNameFontSize}" />
            <TextBlock
                Margin="2,0,0,0"
                x:Name="ScreenInstructionsTextBox"
                Grid.Row="1"
                VerticalAlignment="Center"
                HorizontalAlignment="Left"
                Text="Screen Instructions"
                FontSize="{DynamicResource ScreenInstructionsFontSize}" />
        </Grid>
    </Grid>
</UserControl>
