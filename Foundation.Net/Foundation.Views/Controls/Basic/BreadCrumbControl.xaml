<UserControl
    x:Class="Foundation.Views.BreadCrumbControl"
    Name="ThisControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

    mc:Ignorable="d"

    HorizontalAlignment="{Binding ElementName=ThisControl, Path=HorizontalAlignment}"
    VerticalAlignment="{Binding ElementName=ThisControl, Path=VerticalAlignment}"
    >

    <StackPanel
        Orientation="Horizontal">
        <Polygon
            Fill="{Binding ElementName=ThisControl, Path=BackColour}"
            Points="0,0 13,0 13,26 0,26 13,13"
        />
        <Label
            VerticalContentAlignment="Center"
            HorizontalContentAlignment="Center"
            Background="{Binding ElementName=ThisControl, Path=BackColour}"
            Content="{Binding ElementName=ThisControl, Path=Text1}"
            FontFamily="{DynamicResource DefaultApplicationFontFamily}"
            FontSize="{DynamicResource BreadCrumbFontSize}"
        />
        <Polygon
            Fill="{Binding ElementName=ThisControl, Path=BackColour}"
            Points="0,0 13,13 0,26"
        />
    </StackPanel>
</UserControl>
