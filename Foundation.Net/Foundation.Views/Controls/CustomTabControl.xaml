<TabControl
    x:Class="Foundation.Views.CustomTabControl"
    Name="ThisControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

    mc:Ignorable="d"

    xmlns:c="clr-namespace:Foundation.Common;assembly=Foundation.Common"

    d:DesignHeight="150" d:DesignWidth="150"
    >

    <TabControl.Resources>
        <Style TargetType="{x:Type TabItem}">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0" />
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <Border x:Name="grid"
                                Margin="-2"
                                Background="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type Control}},Path=Background}">
                            <ContentPresenter>
                                <ContentPresenter.Content>
                                    <TextBlock
                                        Name="textBlock"
                                        Margin="4"
                                        FontFamily="{DynamicResource DefaultFixedFontFamily}"
                                        FontSize="{DynamicResource DefaultFontSize}"
                                        Text="{TemplateBinding Content}"
                                        Background="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type Control}},Path=Background}"/>
                                </ContentPresenter.Content>
                            </ContentPresenter>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Mode=FindAncestor,AncestorType={x:Type TabItem}},Path=Header}" Value="Information">
                                <Setter TargetName="grid" Property="Background" Value="PaleGreen"/>
                                <Setter TargetName="textBlock"  Property="Background" Value="PaleGreen"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </TabControl.Resources>

    <Grid Background="PaleGreen" Margin="-4" />

</TabControl>
