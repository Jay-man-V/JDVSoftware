<UserControl
    x:Class="Foundation.Views.AvailableSelectedControl"
    Name="ThisControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    
    xmlns:controls="clr-namespace:Foundation.Views"

    mc:Ignorable="d" 
    d:DesignHeight="450"
    d:DesignWidth="800"
    HorizontalAlignment="{Binding ElementName=ThisControl, Path=HorizontalAlignment}"
    VerticalAlignment="{Binding ElementName=ThisControl, Path=VerticalAlignment}"
   >

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="87" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="24" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <controls:LabelControl Grid.Row="0" Grid.Column="0" Content="Available" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <controls:LabelControl Grid.Row="0" Grid.Column="2" Content="Selected" HorizontalAlignment="Center" VerticalAlignment="Center"/>

        <ListBox Name="AvailableItemsListbox" Grid.Row="1" Grid.Column="0"/>

        <StackPanel Grid.Column="1" Grid.Row="1" Orientation="Vertical" VerticalAlignment="Center">
            <controls:ButtonControl x:Name="AddAllButton" Content="&gt;&gt;" Width="75" Margin="0,3,0,3"/>
            <controls:ButtonControl x:Name="AddSelectedButton"  Content="&gt;" Width="75" Margin="0,3,0,3"/>
            <controls:ButtonControl x:Name="RemoveSelectedButton" Content="&lt;" Width="75" Margin="0,3,0,3"/>
            <controls:ButtonControl x:Name="RemoveAllButton" Content="&lt;&lt;" Width="75" Margin="0,3,0,3"/>
        </StackPanel>

        <ListBox Name="SelectedItemsListbox" Grid.Row="1" Grid.Column="2"/>

    </Grid>
</UserControl>
