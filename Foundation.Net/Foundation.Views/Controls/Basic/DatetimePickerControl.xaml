<DatePicker
    x:Class="Foundation.Views.DatetimePickerControl"
    Name="ThisControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    mc:Ignorable="d"
    HorizontalAlignment="{Binding ElementName=ThisControl, Path=HorizontalAlignment}"
    VerticalAlignment="{Binding ElementName=ThisControl, Path=VerticalAlignment}"

    d:DesignHeight="23" d:DesignWidth="149"
    >

    <DatePicker.Resources>
        <Style TargetType="{x:Type DatePickerTextBox}">
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox
                                Width="19"
                                HorizontalAlignment="Left"
                                VerticalContentAlignment="Center"
                                IsChecked="True" />
                            <TextBox Width="{Binding Path=Width, RelativeSource={RelativeSource AncestorType={x:Type DatePicker}}}"
                                     Text="{Binding Path=SelectedDate,
                                                RelativeSource={RelativeSource AncestorType={x:Type DatePicker}},
                                                Converter={StaticResource DateTimeToStringConverter},
                                                ConverterParameter='dd-MMM-yyyy HH:mm', Mode=OneWay}" RenderTransformOrigin="0.5,0.5"
                            >
                                <TextBox.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform/>
                                        <SkewTransform AngleY="-0.066"/>
                                        <RotateTransform/>
                                        <TranslateTransform Y="-0.086"/>
                                    </TransformGroup>
                                </TextBox.RenderTransform>
                            </TextBox>
                        </StackPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </DatePicker.Resources>
</DatePicker>
<!--
<TextBlock>
    <TextBlock.Resources>
        <local:DateCultureConverter x:Key="converter" />
    </TextBlock.Resources>
    <TextBlock.Text>
        <MultiBinding Converter="{StaticResource converter}">
            <Binding Path="Date" />
            <Binding Path="Settings.Culture" />
        </MultiBinding>
    </TextBlock.Text>
    </TextBlock>
-->
    