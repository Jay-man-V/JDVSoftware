<Expander
    x:Class="Foundation.Views.ExpanderControl"
    Name="ThisControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
    mc:Ignorable="d"

    HorizontalAlignment="{Binding ElementName=ThisControl, Path=HorizontalAlignment}"
    VerticalAlignment="{Binding ElementName=ThisControl, Path=VerticalAlignment}"
>
    <b:Interaction.Triggers>
        <b:EventTrigger EventName="Expanded">
            <b:InvokeCommandAction Command="{Binding ElementName=ThisControl, Path=ExpandedCommand}"/>
        </b:EventTrigger>
        <b:EventTrigger EventName="Collapsed">
            <b:InvokeCommandAction Command="{Binding ElementName=ThisControl, Path=ExpandedCommand}"/>
        </b:EventTrigger>
    </b:Interaction.Triggers>

</Expander>
