<Window
    x:Class="Foundation.Views.ProgressBarForm"
    Name="ThisWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    
    xmlns:controls="clr-namespace:Foundation.Views"

    mc:Ignorable="d"

    Height="Auto" Width="600"
    BorderThickness="1"
    WindowStyle="None"
    SizeToContent="Height"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen"
    >

    <Window.Resources>
    </Window.Resources>
    
    <Grid Margin="0,0,0,5">
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="*"/>
            <RowDefinition Height="27" />
            <RowDefinition Height="27" />
        </Grid.RowDefinitions>

        <controls:ScreenTitleControl Grid.Row="0" ScreenName="{Binding Product}" ScreenInstructions="{Binding CompanyName}" MessageBoxImage="None" />

        <GroupBox Grid.Row="1" Margin="5,0,5,5" Header="Progress Report" Visibility="{Binding ProgressReportVisibility}" >
            <StackPanel>
                <controls:TextBoxControl
                    Height="200"
                    IsReadOnly="True"
                    Text="{Binding ProgressReport, Mode=OneWay}"
                    TextAlignment="Left"
                    VerticalScrollBarVisibility="Auto"
                    />
            </StackPanel>
        </GroupBox>

        <controls:ProgressBarControl
            Grid.Row="2"
            Margin="0,0,0,0"
            IsIndeterminate="True"
            Visibility="{Binding ProgressBarVisibility}"
            />
        <controls:ButtonControl
            Grid.Row="3"
            Margin="0,5,0,0"
            Width="100"
            Visibility="{Binding CancelButtonVisibility}"
            Command="{Binding CancelCommand}"
            CommandParameter="{Binding ElementName=ThisWindow}"
            Content="_Cancel"
            />
    </Grid>
</Window>
