<Button
    x:Class="Foundation.Views.ToggleButtonControl"
    Name="ThisControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"

    xmlns:local="clr-namespace:Foundation.Views"

    HorizontalAlignment="{Binding ElementName=ThisControl, Path=HorizontalAlignment}"
    VerticalAlignment="{Binding ElementName=ThisControl, Path=VerticalAlignment}"
    mc:Ignorable="d"
    d:DesignHeight="25"
    d:DesignWidth="75"
    >

    <Button.Resources>
        <SolidColorBrush x:Key="MouseOverBackgroundKey" Color="Chocolate" />
        <SolidColorBrush x:Key="MouseOverForegroundKey" Color="Yellow" />
    </Button.Resources>

    <Button.Style>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="Black"
                            BorderThickness="1">
                            <ContentPresenter
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Content="{Binding ElementName=ThisControl, Path=Content}"
                                RecognizesAccessKey="True" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type local:ToggleButtonControl}}}" Value="true">
                    <Setter Property="Background" Value="{DynamicResource MouseOverBackgroundKey}" />
                    <Setter Property="Foreground" Value="{DynamicResource MouseOverForegroundKey}" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Button.Style>
</Button>
